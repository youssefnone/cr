env:
    # Cirrus Specific Vars (Do *NOT* Change)
    CIRRUS_SHELL: bash

task:
  name: "Builder"
  timeout_in: 120m
  container:
      image: ghcr.io/sushrut1101/docker:arch
      cpu: 10
      memory: 32G

  Sync_script:
      - curl -sL https://raw.githubusercontent.com/youssefnone/cr/main/download.sh | bash
